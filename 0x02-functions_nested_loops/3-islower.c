#include "main.h"

/**
 * _islower - checks for letter casing
 *
 * Return: 1 if lowercase and 0 if uppercase
 */
int _islower(int c)
{
	int letter;

	if (letter >= 'a' && letter <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
